#!/usr/bin/env bash
set -e

echo "Embedded chart packaging has been removed."
echo "Configure --managed-chart-name, --managed-chart-repo, and --managed-chart-version (or chart values equivalents) instead."
